package hackathon.d2hd.getGoingApp.controller;

import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RestController;

@RestController
@RequestMapping("/api")
public class FreemiumController {
    /*
    1. Call Hashscraper API to search for "#forSale" which should return a list of Tweet
    2. Call the tweetListToTweetDtoList API to convert the tweetList into a tweetDtoList
    3. Call the
     */

}
